version: 0.0
os: linux
files:
  - source: /docker-compose.yml
    destination: /home/ec2-user/
  - source: /nginx.conf
    destination: /home/ec2-user/
hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: ec2-user
  
